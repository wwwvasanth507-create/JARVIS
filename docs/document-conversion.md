# Document Format Conversion

- Supported safe conversions: `CSV -> JSON`, `JSON -> Markdown`, `Markdown -> DOCX`, `Text -> Markdown`.
- Uses `DocumentConverter` with atomic creation and format validation.
- Does not claim visual fidelity for binary formats where exact rendering cannot be guaranteed.
