skill:
  id: resume_task
  name: Resume Task
  version: 1.0.0
  description: Resume a paused task.
  category: builtin
  author: JARVIS Core
  intents:
    - scheduler.resume
  tools:
    - scheduler.resume
  permissions:
    - SYSTEM_CONTROL
  risk_level: LOW
  enabled: true

workflow:
  - id: resume_schedule
    action: scheduler.resume
    description: Resume paused task
