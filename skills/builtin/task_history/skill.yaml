skill:
  id: task_history
  name: Task History
  version: 1.0.0
  description: View execution history for scheduled tasks.
  category: builtin
  author: JARVIS Core
  intents:
    - scheduler.history
  tools:
    - scheduler.history
  permissions:
    - SYSTEM_CONTROL
  risk_level: LOW
  enabled: true

workflow:
  - id: view_history
    action: scheduler.history
    description: View execution history for scheduled tasks
