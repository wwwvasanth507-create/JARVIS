skill:
  id: pause_task
  name: Pause Task
  version: 1.0.0
  description: Pause a scheduled or recurring task.
  category: builtin
  author: JARVIS Core
  intents:
    - scheduler.pause
  tools:
    - scheduler.pause
  permissions:
    - SYSTEM_CONTROL
  risk_level: LOW
  enabled: true

workflow:
  - id: pause_schedule
    action: scheduler.pause
    description: Pause scheduled or recurring task
