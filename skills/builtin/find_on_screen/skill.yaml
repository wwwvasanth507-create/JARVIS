skill:
  id: find_on_screen
  name: Find On Screen
  version: 1.0.0
  description: Locate coordinates of a visual element or button on screen.
  category: builtin
  author: JARVIS Core

  aliases:
    - find on screen
    - locate button
    - find element

  intents:
    - screen.find

  tools:
    - screen.find

  permissions:
    - SCREEN_READ

  risk_level: LOW
  enabled: true

workflow:
  - id: find_element
    action: screen.find
    description: Find element coordinates using OCR and geometry matching
