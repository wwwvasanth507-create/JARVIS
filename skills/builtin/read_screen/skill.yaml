skill:
  id: read_screen
  name: Read Screen
  version: 1.0.0
  description: Extract visible text from active window or full screen using local OCR.
  category: builtin
  author: JARVIS Core

  aliases:
    - read screen
    - read screen text
    - what text is on my screen

  intents:
    - screen.read_text

  tools:
    - screen.read_text

  permissions:
    - SCREEN_READ

  risk_level: LOW
  enabled: true

workflow:
  - id: read_screen_txt
    action: screen.read_text
    description: Extract text regions using local OCR engine
