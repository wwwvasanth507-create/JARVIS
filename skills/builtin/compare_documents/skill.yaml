skill:
  id: compare_documents
  name: Compare Documents
  version: 1.0.0
  description: Compare two documents deterministically and semantically.
  category: builtin
  author: JARVIS Core

  aliases:
    - compare documents
    - diff documents
    - compare reports

  intents:
    - document.compare

  tools:
    - document.compare

  permissions:
    - READ_FILES

  risk_level: LOW
  enabled: true

workflow:
  - id: compare_docs
    action: document.compare
    description: Compute structural, section, and value differences between two files
