skill:
  id: create_document
  name: Create Document
  version: 1.0.0
  description: Create a new document with safe atomic writes and overwrite protection.
  category: builtin
  author: JARVIS Core

  aliases:
    - create document
    - write document
    - generate document

  intents:
    - document.create

  tools:
    - document.create

  permissions:
    - WRITE_FILES

  risk_level: MEDIUM
  enabled: true

workflow:
  - id: create_doc
    action: document.create
    description: Perform atomic creation and verification of document file
