skill:
  id: convert_document
  name: Convert Document
  version: 1.0.0
  description: Convert document between supported formats safely.
  category: builtin
  author: JARVIS Core

  aliases:
    - convert document
    - transform document
    - convert notes

  intents:
    - document.convert

  tools:
    - document.convert

  permissions:
    - READ_FILES
    - WRITE_FILES

  risk_level: MEDIUM
  enabled: true

workflow:
  - id: convert_doc
    action: document.convert
    description: Convert document format using safe atomic writes
