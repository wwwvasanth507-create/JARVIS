skill:
  id: extract_table
  name: Extract Table
  version: 1.0.0
  description: Extract tabular data from documents into structured table objects.
  category: builtin
  author: JARVIS Core

  aliases:
    - extract table
    - parse table
    - extract tabular data

  intents:
    - document.extract_table

  tools:
    - document.extract_table

  permissions:
    - READ_FILES

  risk_level: MEDIUM
  enabled: true

workflow:
  - id: extract_tbl
    action: document.extract_table
    description: Parse tables from document and return structured data
