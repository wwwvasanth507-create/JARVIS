# Core tensor framework (PyTorch CPU build)
# To install CPU-only build on Windows:
# pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0

# Configuration and Serialization
PyYAML>=6.0.1

# Numerical operations
numpy>=1.26.0

# Testing and development
pytest>=8.0.0

# HTTP API Server and ASGI runtime (Phase 9)
fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.6.0
httpx>=0.27.0
